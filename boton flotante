<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Botón Flotante - HTML y CSS</title>
    <style>
        /* Estilos básicos para la página */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px 20px;
            background: rgba(255, 255, 255, 0.95);
            margin-top: 50px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        h1 {
            text-align: center;
            color: #826196;
            margin-bottom: 30px;
            font-size: 2.5em;
        }

          h2 {
            text-align: center;
            color: #4a5568;
            margin-bottom: 30px;
            font-size: 2em;
        }


        .content {
            margin-bottom: 30px;
        }

        .content p {
            margin-bottom: 20px;
            font-size: 1.1em;
            text-align: justify;
        }

        .highlight {
            background-color: #e2e8f0;
            padding: 15px;
            border-radius: 8px;
            border-left: 4px solid #667eea;
            margin: 20px 0;
        }

        .code-example {
            background-color: #2d3748;
            color: #e2e8f0;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            overflow-x: auto;
            font-family: 'Courier New', monospace;
            font-size: 14px;
        }

        /* AQUÍ COMIENZA EL BOTÓN FLOTANTE */
        .floating-button {
            /* POSICIÓN FIJA - La clave principal */
            position: fixed;
            bottom: 30px;    /* 30px desde abajo */
            right: 30px;     /* 30px desde la derecha */
            
            /* TAMAÑO Y FORMA */
            width: 60px;
            height: 60px;
            border-radius: 50%;  /* Hace el círculo perfecto */
            
            /* APARIENCIA */
            background: linear-gradient(45deg, #ff6b6b, #ee5a52);
            border: none;
            color: white;
            font-size: 24px;
            
            /* EFECTOS */
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
            cursor: pointer;
            
            /* LAYERING - Aparece encima de todo */
            z-index: 1000;
            
            /* TRANSICIONES SUAVES */
            transition: all 0.3s ease;
            
            /* CENTRAR CONTENIDO */
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* EFECTOS AL PASAR EL MOUSE */
        .floating-button:hover {
            transform: scale(1.1);  /* Crece 10% */
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.4);
            background: linear-gradient(45deg, #ee5a52, #ff6b6b);
        }

        /* EFECTO AL HACER CLIC */
        .floating-button:active {
            transform: scale(0.95);  /* Se reduce momentáneamente */
        }

        /* ANIMACIÓN DE PULSO - Para llamar la atención */
        .floating-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border-radius: 50%;
            border: 2px solid #ff6b6b;
            animation: pulse 2s infinite;
            opacity: 0;
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
                opacity: 1;
            }
            100% {
                transform: scale(1.5);
                opacity: 0;
            }
        }

        /* DISEÑO RESPONSIVO */
        @media (max-width: 768px) {
            .floating-button {
                bottom: 20px;
                right: 20px;
                width: 50px;
                height: 50px;
                font-size: 20px;
            }
        }

        /* CONTENIDO EXTRA PARA MOSTRAR EL SCROLL */
        .extra-content {
            height: 800px;
            background: linear-gradient(180deg, #f7fafc 0%, #edf2f7 100%);
            margin-top: 30px;
            padding: 40px;
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5em;
            color: #4a5568;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Botón Flotante con HTML y CSS</h1>
        <h2>German-Nadia-Denis-Juanmanuel</h2>
    
        
        <div class="content">
            <p>Este es un ejemplo práctico de cómo crear un <strong>botón flotante</strong> usando únicamente HTML y CSS. El botón permanece fijo en la pantalla sin importar el scroll.</p>
            
            <div class="highlight">
                <strong>Propiedades CSS clave:</strong><br>
                • <code>position: fixed</code> - Posición fija respecto al viewport<br>
                • <code>bottom: 30px</code> - Distancia desde abajo<br>
                • <code>right: 30px</code> - Distancia desde la derecha<br>
                • <code>z-index: 1000</code> - Aparece por encima de otros elementos
            </div>

            <div class="code-example">
.floating-button {
    position: fixed;     /* ¡La propiedad clave! */
    bottom: 30px;        /* Posición desde abajo */
    right: 30px;         /* Posición desde la derecha */
    width: 60px;         /* Ancho del botón */
    height: 60px;        /* Alto del botón */
    border-radius: 50%;  /* Forma circular */
    z-index: 1000;       /* Por encima de todo */
}
            </div>
            
            <p><strong>¿Por qué usar position: fixed?</strong><br>
            A diferencia de <code>position: absolute</code>, el elemento con <code>position: fixed</code> se posiciona respecto al viewport (ventana del navegador), no respecto a su contenedor padre. Esto hace que permanezca en el mismo lugar incluso cuando el usuario hace scroll.</p>
            
            <div class="highlight">
                <strong>Efectos implementados solo con CSS:</strong><br>
                • Transiciones suaves con <code>transition</code><br>
                • Efectos hover con <code>:hover</code><br>
                • Animación de pulso con <code>@keyframes</code><br>
                • Diseño responsivo con <code>@media queries</code>
            </div>
        </div>
    </div>

    <div class="extra-content">
        <div>
            <p>Haz scroll para ver cómo el botón flotante permanece fijo</p>
            <p style="margin-top: 20px; font-size: 0.9em; opacity: 0.7;">
                El botón rojo siempre estará visible en la esquina inferior derecha
            </p>
        </div>
    </div>

    <!-- EL BOTÓN FLOTANTE -->
    <button class="floating-button" title="Botón Flotante">
        💬
    </button>

</body>
</html>
